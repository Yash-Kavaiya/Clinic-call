{
  "name": "transfer_to_human",
  "description": "End the voicebot so Exotel Connect hunt group can take the call.",
  "method": "POST",
  "url": "{{BASE_URL}}/tools/transfer_to_human",
  "headers": { "X-Tool-Key": "{{TOOL_API_KEY}}", "Content-Type": "application/json" },
  "body": { "reason": "caller asked for receptionist", "department": "reception", "call_sid": "{{call_sid}}" }
}
